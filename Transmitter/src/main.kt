import java.io.FileInputStream

fun main(){
    //val fin = FileInputStream("input.txt");
    //val buffer = ByteArray(fin.available())
    //fin.read(buffer, 0, fin.available())
    ////println(buffer)
    //var text = ""
    //    for (i in buffer.indices) {
    //        text += (buffer[i].toChar())
    //    }
    //println(text)  //commented because takes a lot of time, needs to be configured
        Transmitter.send("IAMCS")
    }